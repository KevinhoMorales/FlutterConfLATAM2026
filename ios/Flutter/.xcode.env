#!/bin/sh
# Versioned file sourced by Xcode script phases.
# Override locally with an untracked .xcode.env.local if needed.

flutter_bin="$(command -v flutter || true)"
if [ -n "$flutter_bin" ]; then
  export FLUTTER_ROOT="$(cd "$(dirname "$flutter_bin")/.." && pwd)"
fi
